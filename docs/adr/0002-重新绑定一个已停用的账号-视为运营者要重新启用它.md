# ADR 0002 — 重新绑定一个已停用的账号，视为运营者要重新启用它

**Status:** accepted

重绑会把新凭证写进 auth 文件，同时保留运营者手设的字段。`disabled` 是这条保留规则的唯一例外：它不会被带到新文件里，auth 文件合成器读不到该键就按可用处理，账号因此自动恢复。

理由是意图：运营者会去重绑一个停用账号，几乎总是因为想让它重新工作；让人重绑完还要再手工启用一次，是多余的一步。

这条规则住在 `misc.ApplyPreservedMetadata` 的不保留名单里，那份名单目前只有这一个键。
